<UserControl x:Class="X3DCcdInspector.Views.CcdPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:X3DCcdInspector.Views">
    <Grid Margin="4,2">
        <!-- Accent left edge — identity stripe (V-Cache=green, Frequency=blue) -->
        <Border CornerRadius="8,0,0,8" Width="3" HorizontalAlignment="Left"
                Background="{Binding BadgeBackground}" Opacity="0.5"/>

        <!-- Main panel -->
        <Border CornerRadius="8"
                Background="{StaticResource BgSecondaryBrush}"
                BorderBrush="{Binding BorderBrush}"
                BorderThickness="{Binding BorderThickness}"
                Padding="12,8" Margin="2,0,0,0">
            <DockPanel>
                <!-- Single-line header: CCD0 [V-Cache] 96 MB L3 · Cores 0-15 · role -->
                <DockPanel DockPanel.Dock="Top" Margin="0,0,0,6">
                    <TextBlock Text="{Binding CcdName}" FontSize="14" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               VerticalAlignment="Center"/>
                    <Border Style="{StaticResource BadgeStyle}"
                            Background="{Binding BadgeBackground}">
                        <TextBlock Text="{Binding BadgeText}"
                                   Foreground="{Binding BadgeForeground}"
                                   FontSize="10" FontWeight="SemiBold"/>
                    </Border>
                    <TextBlock VerticalAlignment="Center" Margin="8,0,0,0"
                               FontSize="11" Foreground="{StaticResource TextTertiaryBrush}">
                        <Run Text="{Binding L3SizeText, Mode=OneWay}"/>
                        <Run Text=" · "/>
                        <Run Text="{Binding CoreRangeText, Mode=OneWay}"/>
                    </TextBlock>
                    <TextBlock Text="{Binding RoleLabel}" Style="{StaticResource SmallText}"
                               DockPanel.Dock="Right" HorizontalAlignment="Right"
                               VerticalAlignment="Center"/>
                </DockPanel>

                <!-- Core grid — 4 columns, rows auto-determined -->
                <ItemsControl ItemsSource="{Binding Cores}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="4"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <views:CoreTile/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
