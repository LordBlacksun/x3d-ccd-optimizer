<Window x:Class="X3DCcdOptimizer.Views.ProcessPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Background Processes"
        Width="460" Height="480"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgPrimaryBrush}"
        AutomationProperties.Name="Process picker window">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select processes to pin to the Frequency CCD:"
                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,8"/>

        <ListBox x:Name="ProcessList" Grid.Row="1"
                 Background="{StaticResource BgSecondaryBrush}"
                 BorderBrush="{StaticResource BorderDefaultBrush}"
                 SelectionMode="Multiple"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsSelected}" Margin="2"
                              Foreground="{StaticResource TextPrimaryBrush}">
                        <CheckBox.Content>
                            <TextBlock>
                                <Run Text="{Binding DisplayName, Mode=OneWay}" FontWeight="SemiBold"/>
                                <Run Text="{Binding ExeName, StringFormat=({0}), Mode=OneWay}"
                                     Foreground="{StaticResource TextTertiaryBrush}"/>
                            </TextBlock>
                        </CheckBox.Content>
                    </CheckBox>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="2"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border Background="Transparent" Padding="2">
                                    <ContentPresenter/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <CheckBox Grid.Row="2" Content="Show all processes (remove path filter)"
                  x:Name="ShowAllCheckBox"
                  Foreground="{StaticResource TextTertiaryBrush}" FontSize="11"
                  Margin="0,6,0,0"
                  Checked="OnShowAllChanged" Unchecked="OnShowAllChanged"
                  ToolTip="Shows all processes including those in system directories."
                  AutomationProperties.Name="Show all processes checkbox"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <TextBlock x:Name="CountText" Foreground="{StaticResource TextTertiaryBrush}"
                       FontSize="11" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <Button Content="Confirm" Click="OnConfirm" Style="{StaticResource SubtleButtonStyle}"
                    Width="80" Margin="0,0,8,0" AutomationProperties.Name="Confirm selection"/>
            <Button Content="Cancel" Click="OnCancel" Style="{StaticResource SubtleButtonStyle}"
                    Width="80" AutomationProperties.Name="Cancel selection"/>
        </StackPanel>
    </Grid>
</Window>
